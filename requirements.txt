# ──────────────────────────────────────────────────────────────────
# DocIQ – Production Dependencies
# ──────────────────────────────────────────────────────────────────
# Install:  pip install -r requirements.txt
# Pinned to the minor version (>=X.Y,<X+1) for reproducibility
# while still allowing patch-level updates.
# ──────────────────────────────────────────────────────────────────

# ── Configuration ────────────────────────────────────────────────
pydantic>=2.12,<3
pydantic-settings>=2.13,<3
PyYAML>=6.0,<7

# ── API Server ───────────────────────────────────────────────────
fastapi>=0.115,<1
uvicorn>=0.30,<1

# ── PDF & Image Processing ───────────────────────────────────────
PyMuPDF>=1.25,<2                # fitz – direct PDF text extraction
pdf2image>=1.17,<2              # PDF-to-image via Poppler (for OCR path)
opencv-python-headless>=4.10,<5 # image preprocessing (grayscale, threshold)
numpy>=1.26,<3                  # array ops for OpenCV / image processing
Pillow>=10.0,<13                # PIL image handling for Tesseract

# ── OCR ──────────────────────────────────────────────────────────
pytesseract>=0.3.10,<1          # Tesseract wrapper (requires tesseract binary)

# ── NLP ──────────────────────────────────────────────────────────
langdetect>=1.0.9,<2            # language detection (en/es)

# ── Data Generation (optional – only needed for test PDF creation)
reportlab>=4.0,<5               # synthetic PDF/image generation
